#ifndef pokeArray
#define PokeArray

#include <string>

struct Pokemon {
    std::string name;
    int id;
    // otros atributos
};

extern Pokemon pokemonArray[151];

#endif // POKEMONS_H
